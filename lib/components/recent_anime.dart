import 'package:flutter/material.dart';

class RecentAnime extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Container(
      child: Text('RecentAnime'),
    );
  }
}